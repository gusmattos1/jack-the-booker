<% @reservations.each do |reservation| %> 

	<%= reservation.date %> 
	<%= reservation.time %> 
	<%= reservation.notes %> 
<% end %> 

<% @reserved_restaurants.each do |restaurant| %>
	<%= restaurant.name %> 
	<%= restaurant.address %> 
	<%= restaurant.summary %>
<% end %>

